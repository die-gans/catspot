name: catspot_shared
description: Shared Dart constants and JSON-schema-driven contract types for Catspot.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.12.2

dev_dependencies:
  test: ^1.25.0
