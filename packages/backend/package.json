{
  "name": "@catspot/backend",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Firebase backend for Catspot: Cloud Functions (catch flow, naming, users), Firestore rules/indexes, R2 storage wiring.",
  "scripts": {
    "build": "pnpm --dir functions build",
    "typecheck": "pnpm --dir functions typecheck",
    "deploy": "firebase deploy --project catspot-9ee0d"
  },
  "engines": {
    "node": ">=22.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  }
}
